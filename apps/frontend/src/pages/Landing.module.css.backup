@import url('https://fonts.googleapis.com/css2?family=Heebo:wght@300;400;500;600;700;800;900&display=swap');

/* ── TOKENS ── */
:root {
  --primary:     #2563eb;
  --primary-dark:#1d4ed8;
  --primary-light:#3b82f6;
  --bg:          #ffffff;
  --bg2:         #f8faff;
  --text:        #0f172a;
  --muted:       #64748b;
  --card:        #ffffff;
  --border:      rgba(37,99,235,0.12);
}

* { box-sizing: border-box; margin: 0; padding: 0; }

.page {
  font-family: 'Heebo', sans-serif;
  background: var(--bg);
  color: var(--text);
  direction: rtl;
  min-height: 100vh;
  overflow-x: hidden;
}

/* ── NAVBAR ── */
.navbar {
  position: fixed; top: 0; right: 0; left: 0; z-index: 100;
  display: flex; align-items: center; justify-content: space-between;
  padding: 0 2rem; height: 64px;
  background: rgba(255,255,255,0.85);
  backdrop-filter: blur(16px);
  -webkit-backdrop-filter: blur(16px);
  border-bottom: 1px solid rgba(37,99,235,0.08);
  box-shadow: 0 1px 0 rgba(0,0,0,0.04);
}
.logo { display: flex; align-items: center; gap: 0.4rem; font-size: 1.2rem; font-weight: 800; color: var(--text); text-decoration: none; }
.logoAccent { color: var(--primary); }
.navLinks { display: flex; gap: 2rem; list-style: none; }
.navLinks a { color: var(--muted); text-decoration: none; font-size: 0.9rem; font-weight: 500; transition: color 0.2s; }
.navLinks a:hover { color: var(--primary); }
.navCta { display: flex; gap: 0.6rem; }
.btnOutline {
  padding: 0.45rem 1.1rem; border: 1.5px solid var(--border);
  background: transparent; color: var(--primary); border-radius: 8px;
  font-family: 'Heebo',sans-serif; font-size: 0.88rem; font-weight: 600;
  cursor: pointer; text-decoration: none; display: inline-flex; align-items: center;
  transition: border-color 0.2s, background 0.2s;
}
.btnOutline:hover { border-color: var(--primary); background: rgba(37,99,235,0.04); }
.btnPrimary {
  padding: 0.45rem 1.1rem; background: var(--primary); color: #fff; border: none;
  border-radius: 8px; font-family: 'Heebo',sans-serif; font-size: 0.88rem; font-weight: 700;
  cursor: pointer; text-decoration: none; display: inline-flex; align-items: center;
  transition: background 0.2s, transform 0.15s;
}
.btnPrimary:hover { background: var(--primary-dark); transform: translateY(-1px); }

/* ── HERO ── */
.hero {
  min-height: 100vh;
  display: flex; align-items: center; justify-content: center;
  text-align: center; padding: 7rem 1.5rem 5rem;
  position: relative; overflow: hidden;
  background: #f0f6ff;
}

/* Aurora blobs */
.auroraWrap {
  position: absolute; inset: 0; overflow: hidden; z-index: 0;
}
.blob {
  position: absolute; border-radius: 50%;
  filter: blur(80px); opacity: 0.55;
  animation: blobFloat 12s ease-in-out infinite alternate;
}
.blob1 {
  width: 600px; height: 600px;
  background: radial-gradient(circle, #93c5fd 0%, #3b82f6 60%, transparent 100%);
  top: -150px; right: -100px;
  animation-duration: 14s;
}
.blob2 {
  width: 500px; height: 500px;
  background: radial-gradient(circle, #bfdbfe 0%, #60a5fa 60%, transparent 100%);
  bottom: -100px; left: -80px;
  animation-duration: 18s; animation-delay: -4s;
}
.blob3 {
  width: 350px; height: 350px;
  background: radial-gradient(circle, #dbeafe 0%, #93c5fd 70%, transparent 100%);
  top: 40%; left: 40%;
  animation-duration: 10s; animation-delay: -8s;
}
@keyframes blobFloat {
  0%   { transform: translate(0, 0) scale(1); }
  33%  { transform: translate(30px, -40px) scale(1.05); }
  66%  { transform: translate(-20px, 20px) scale(0.97); }
  100% { transform: translate(15px, -15px) scale(1.03); }
}

/* subtle dot-grid overlay */
.gridOverlay {
  position: absolute; inset: 0;
  background-image: radial-gradient(rgba(37,99,235,0.12) 1px, transparent 1px);
  background-size: 32px 32px;
  mask-image: radial-gradient(ellipse 80% 80% at 50% 50%, black 40%, transparent 100%);
  -webkit-mask-image: radial-gradient(ellipse 80% 80% at 50% 50%, black 40%, transparent 100%);
}

.heroContent {
  position: relative; z-index: 1; max-width: 780px;
  animation: fadeUp 0.9s cubic-bezier(0.16,1,0.3,1) both;
}
@keyframes fadeUp {
  from { opacity: 0; transform: translateY(32px); }
  to   { opacity: 1; transform: translateY(0); }
}

.heroTag {
  display: inline-block;
  background: rgba(37,99,235,0.08); border: 1px solid rgba(37,99,235,0.2);
  color: var(--primary); font-size: 0.8rem; font-weight: 600;
  padding: 0.3rem 0.9rem; border-radius: 100px; margin-bottom: 1.5rem;
}

.heroTitle {
  font-size: clamp(2.4rem, 6vw, 4.2rem);
  font-weight: 900; line-height: 1.1;
  color: #0f172a; margin-bottom: 1.25rem;
}
.heroGradientText {
  background: linear-gradient(135deg, #2563eb 0%, #60a5fa 100%);
  -webkit-background-clip: text; -webkit-text-fill-color: transparent;
  background-clip: text;
}

.heroSub {
  font-size: clamp(1rem, 2vw, 1.15rem);
  color: var(--muted); line-height: 1.75;
  max-width: 560px; margin: 0 auto 2.5rem;
}

.heroButtons { display: flex; gap: 1rem; justify-content: center; flex-wrap: wrap; }

.heroPrimary {
  padding: 0.9rem 2.2rem; background: var(--primary); color: #fff; border: none;
  border-radius: 12px; font-family: 'Heebo',sans-serif; font-size: 1rem; font-weight: 700;
  cursor: pointer; text-decoration: none; display: inline-block;
  box-shadow: 0 4px 20px rgba(37,99,235,0.35);
  transition: background 0.2s, transform 0.15s, box-shadow 0.2s;
}
.heroPrimary:hover { background: var(--primary-dark); transform: translateY(-2px); box-shadow: 0 8px 28px rgba(37,99,235,0.45); }

.heroOutline {
  padding: 0.9rem 2rem; background: rgba(255,255,255,0.7);
  color: var(--text); border: 1.5px solid rgba(37,99,235,0.2);
  border-radius: 12px; font-family: 'Heebo',sans-serif; font-size: 1rem; font-weight: 600;
  cursor: pointer; text-decoration: none; display: inline-block;
  backdrop-filter: blur(8px);
  transition: border-color 0.2s, color 0.2s, background 0.2s;
}
.heroOutline:hover { border-color: var(--primary); color: var(--primary); background: rgba(37,99,235,0.04); }

/* ── STATS ── */
.stats {
  background: #fff;
  border-top: 1px solid rgba(37,99,235,0.08);
  border-bottom: 1px solid rgba(37,99,235,0.08);
  padding: 2.5rem 2rem;
}
.statsGrid {
  max-width: 900px; margin: 0 auto;
  display: grid; grid-template-columns: repeat(4,1fr); gap: 2rem; text-align: center;
}
.statItem { display: flex; flex-direction: column; align-items: center; gap: 0.3rem; }
.statNum { font-size: clamp(1.8rem,3.5vw,2.6rem); font-weight: 900; color: var(--primary); }
.statLabel { font-size: 0.88rem; color: var(--muted); font-weight: 500; }

/* ── SECTION SHARED ── */
.section { padding: 5.5rem 2rem; max-width: 1100px; margin: 0 auto; }
.sectionTag {
  color: var(--primary); font-size: 0.72rem; font-weight: 700;
  letter-spacing: 2.5px; text-transform: uppercase; margin-bottom: 0.6rem; display: block;
}
.sectionTitle { font-size: clamp(1.6rem,3vw,2.3rem); font-weight: 800; margin-bottom: 3rem; color: var(--text); }

/* ── STEPS ── */
.stepsGrid { display: grid; grid-template-columns: repeat(3,1fr); gap: 1.5rem; }
.stepCard {
  background: var(--bg2); border: 1px solid rgba(37,99,235,0.1);
  border-radius: 20px; padding: 2rem 1.75rem;
  transition: transform 0.25s, box-shadow 0.25s, border-color 0.25s;
}
.stepCard:hover { transform: translateY(-5px); box-shadow: 0 12px 40px rgba(37,99,235,0.1); border-color: rgba(37,99,235,0.25); }
.stepNum {
  display: inline-flex; align-items: center; justify-content: center;
  width: 36px; height: 36px; border-radius: 10px;
  background: rgba(37,99,235,0.1); color: var(--primary);
  font-size: 0.85rem; font-weight: 800; margin-bottom: 1rem;
}
.stepIcon { font-size: 2rem; margin-bottom: 0.75rem; display: block; }
.stepTitle { font-size: 1.05rem; font-weight: 700; margin-bottom: 0.5rem; color: var(--text); }
.stepDesc { font-size: 0.88rem; color: var(--muted); line-height: 1.65; }

/* ── ROLES ── */
.rolesBg { background: var(--bg2); padding: 5.5rem 2rem; }
.rolesInner { max-width: 1100px; margin: 0 auto; }
.rolesGrid { display: grid; grid-template-columns: repeat(4,1fr); gap: 1.25rem; }
.roleCard {
  background: #fff; border: 1px solid rgba(37,99,235,0.1);
  border-radius: 20px; padding: 1.75rem 1.5rem;
  transition: transform 0.25s, box-shadow 0.25s, border-color 0.25s;
}
.roleCard:hover { transform: translateY(-4px); box-shadow: 0 12px 36px rgba(37,99,235,0.1); border-color: rgba(37,99,235,0.3); }
.roleIcon { font-size: 2rem; margin-bottom: 0.75rem; display: block; }
.roleTitle { font-size: 1rem; font-weight: 700; margin-bottom: 0.75rem; color: var(--text); }
.roleBullets { list-style: none; padding: 0; }
.roleBullets li {
  font-size: 0.84rem; color: var(--muted); margin-bottom: 0.4rem;
  display: flex; align-items: flex-start; gap: 0.4rem;
}
.roleBullets li::before { content: '→'; color: var(--primary); flex-shrink: 0; }

/* ── CTA ── */
.ctaSection {
  padding: 7rem 2rem; text-align: center;
  background: linear-gradient(135deg, #eff6ff 0%, #dbeafe 50%, #eff6ff 100%);
  position: relative; overflow: hidden;
}
.ctaContent { position: relative; z-index: 1; max-width: 600px; margin: 0 auto; }
.ctaTitle { font-size: clamp(1.8rem,4vw,3rem); font-weight: 900; margin-bottom: 1rem; color: var(--text); }
.ctaSub { font-size: 1rem; color: var(--muted); margin-bottom: 2rem; line-height: 1.6; }
.ctaBtn {
  display: inline-block; padding: 1rem 2.5rem;
  background: var(--primary); color: #fff; border: none;
  border-radius: 14px; font-family: 'Heebo',sans-serif; font-size: 1.05rem; font-weight: 800;
  cursor: pointer; text-decoration: none;
  box-shadow: 0 6px 28px rgba(37,99,235,0.4);
  transition: background 0.2s, transform 0.15s, box-shadow 0.2s;
  margin-bottom: 1rem; display: block; width: fit-content; margin: 0 auto 1rem;
}
.ctaBtn:hover { background: var(--primary-dark); transform: translateY(-3px); box-shadow: 0 12px 40px rgba(37,99,235,0.5); }
.ctaNote { font-size: 0.82rem; color: var(--muted); }

/* ── FOOTER ── */
.footer { background: #f8fafc; border-top: 1px solid rgba(37,99,235,0.08); padding: 3rem 2rem 2rem; }
.footerInner {
  max-width: 1100px; margin: 0 auto;
  display: flex; justify-content: space-between; align-items: flex-start;
  gap: 2rem; flex-wrap: wrap; margin-bottom: 2rem;
}
.footerLogo { font-size: 1.1rem; font-weight: 800; color: var(--primary); margin-bottom: 0.5rem; }
.footerDesc { font-size: 0.84rem; color: var(--muted); max-width: 260px; line-height: 1.6; }
.footerLinks { display: flex; gap: 1.5rem; flex-wrap: wrap; align-items: center; }
.footerLinks a { color: var(--muted); text-decoration: none; font-size: 0.88rem; transition: color 0.2s; }
.footerLinks a:hover { color: var(--primary); }
.footerCopy { max-width: 1100px; margin: 0 auto; padding-top: 1.5rem; border-top: 1px solid rgba(0,0,0,0.06); font-size: 0.78rem; color: var(--muted); text-align: center; }

/* ── RESPONSIVE ── */
@media (max-width: 900px) {
  .stepsGrid, .rolesGrid { grid-template-columns: repeat(2,1fr); }
  .statsGrid { grid-template-columns: repeat(2,1fr); }
  .navLinks { display: none; }
}
@media (max-width: 600px) {
  .stepsGrid, .rolesGrid, .statsGrid { grid-template-columns: 1fr; }
  .navbar { padding: 0 1rem; }
}

/* Numbers rain overlay — keeps content readable */
.heroOverlay {
  position: absolute; inset: 0; z-index: 1;
  background: radial-gradient(ellipse 75% 70% at 50% 50%,
    rgba(240,246,255,0.92) 0%,
    rgba(219,234,254,0.75) 50%,
    rgba(219,234,254,0.4) 100%);
}
.logoImg { width: 32px; height: 32px; border-radius: 8px; object-fit: cover; flex-shrink: 0; }
.footerLogo { display: flex; align-items: center; gap: 0.5rem; font-size: 1.1rem; font-weight: 800; color: var(--primary); margin-bottom: 0.5rem; }
